import java.time.Duration;

public class CustomTimeUnit {
    public static final Duration SECONDS = Duration.ofSeconds(1);
    public static final Duration MINUTES = Duration.ofMinutes(1);
    public static final Duration HOURS = Duration.ofHours(1);
}